import { NavLink, Outlet } from 'react-router-dom';
import { Activity, Box, CheckCircle2, Download, Gauge, Heart, LifeBuoy, Settings, Wrench } from 'lucide-react';
import { runtimeEnvironment } from '../services/runtime/environment';
const items=[['/','Dashboard',Gauge],['/tools','Ferramentas',Wrench],['/validation','Validação',CheckCircle2],['/installations','Instalações',Box],['/favorites','Favoritos',Heart],['/support','Suporte',LifeBuoy],['/download','Download',Download],['/settings','Configurações',Settings]] as const;
export function Shell(){return <div className="shell"><aside className="sidebar"><div className="brand"><div className="brand-mark"><Activity size={19}/></div><div><strong>CENTRAL SOS</strong><small>Suporte técnico</small></div></div><div className="nav-caption">ESPAÇO DE TRABALHO</div><nav>{items.map(([to,label,Icon])=><NavLink key={to} to={to} end={to==='/'} className={({isActive})=>`nav-link ${isActive?'active':''}`}><Icon size={17}/>{label}</NavLink>)}</nav><div className="sidebar-bottom"><i className="online-dot"/> Ambiente {runtimeEnvironment}<span className="version">v0.1.0</span></div></aside><main className="main"><header className="topbar">Central de operações<span>Ambiente {runtimeEnvironment}</span></header><div className="page"><Outlet/></div></main></div>}
